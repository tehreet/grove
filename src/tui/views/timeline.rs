//! Timeline / Gantt view — horizontal bars per agent showing activity over time.
use ratatui::Frame;
use crate::tui::app::App;

pub fn render(f: &mut Frame, app: &mut App) {
    // Builder 3 implements this
    let _ = (f, app);
}
